import React, { Component } from 'react';

const HeaderBar = ( props ) => (
    <div id="header" className="breadcrumb flat">
        <a href="#" className="active">General Information</a>
        <a href="#">Package Details</a>
    </div>
);

export default HeaderBar;



